export type AuthType = {
    loginOrEmail: string
    password:string
}

export type RegistrationInfoType ={
    "login": string
    "password": string
    "email": string
}

export type EmailConfirmationCode = {
    code: string
}
